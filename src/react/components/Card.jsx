import axios from "axios";
import React, { useState, useEffect } from "react";

const Card = ({ timezone }) => {
  
  useEffect(()=>{
    axios.put
  },[])
  return (
    <div className="card-container">
      <p>{timezone}</p>
    </div>
  );
};

export default Card;
